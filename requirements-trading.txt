# Faz A — backtest + strategy dev dependencies.
#
# These are NOT loaded into the production bot.py service (Render loads only
# requirements.txt). Install into a separate venv:
#
#   python -m venv .venv-trading
#   source .venv-trading/bin/activate
#   pip install -r requirements-trading.txt -r requirements-dev.txt
#
# Freqtrade itself is intentionally NOT listed here. Faz A validates a
# hypothesis (time-series momentum + volatility targeting) against Binance
# perpetual data using our own deterministic harness. When Faz A produces a
# GO decision and we start Faz C (testnet execution), we will add
# freqtrade>=2024.x to this file and wrap trading.strategies.tsmom under
# a freqtrade IStrategy adapter. Keeping the dep out until then avoids
# ~200MB of transitive baggage (ccxt, sklearn, sqlalchemy, plotly) during
# the research phase.

pandas>=2.2,<3.0
numpy>=1.26,<3.0
pyarrow>=15.0
